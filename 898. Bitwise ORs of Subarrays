class Solution:
    def subarrayBitwiseORs(self, arr: List[int]) -> int:
        res = set()
        prev = set()
        
        for num in arr:
            cur = {num}
            for x in prev:
                cur.add(x | num)
            res.update(cur)
            prev = cur
        
        return len(res)
